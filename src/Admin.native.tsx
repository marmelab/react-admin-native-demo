import { NativeRouter } from "react-router-native";
import { CommonAdmin } from "./CommonAdmin";

export const Admin = () => {
  return (
    <NativeRouter>
      <CommonAdmin />
    </NativeRouter>
  );
};
